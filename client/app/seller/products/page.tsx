import { ProductsHeader, ProductsList } from "@/features/seller";

const Products = () => {
  return (
    <div>
      <ProductsHeader />
      <ProductsList />
    </div>
  );
};

export default Products;
